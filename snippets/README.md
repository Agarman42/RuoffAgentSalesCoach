# Snippets

Reference HTML/JS fragments only. **Source of truth is always the live files:**

- Profile modal → `index.html` (`#user-profile-modal`)
- Newsletter UI → `index.html` + `js/features/newsletter-generator.js`

Do not load these files at runtime unless explicitly wired in `index.html`.